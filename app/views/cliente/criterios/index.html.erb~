<h1>Listing criterios</h1>

<table>
  <tr>
    <th>Descripcion</th>
    <th>Story</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @criterios.each do |criterio| %>
  <tr>
    <td><%= criterio.descripcion %></td>
    <td><%= criterio.story_id %></td>
    <td><%= link_to 'Show', criterio %></td>
    <td><%= link_to 'Edit', edit_criterio_path(criterio) %></td>
    <td><%= link_to 'Destroy', criterio, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Criterio', new_cliente_criterio_path %>
