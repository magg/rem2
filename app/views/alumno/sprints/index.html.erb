<h1>SPRINTS</h1>
</br>
<table class="table table-striped table-bordered table-condensed" style="table-layout:fixed;word-wrap:break-word;">
<thead>
  <tr>
	<th style="text-align:center;">ID</th>
    <th style="text-align:center;">Comentario</th>
    <th style="text-align:center;">Fecha inicio</th>
    <th style="text-align:center;">Fecha fin</th>
    <th></th>
    <th></th>
  </tr>
</thead>
<tbody>
<% @sprints.each do |sprint| %>
  <tr>
	<td style="text-align:center;"><%= link_to sprint.id, [:alumno, sprint]  %></td>
    <td><%= sprint.comentario %></td>
    <td style="text-align:center;"><%= sprint.fechainicio %></td>
    <td style="text-align:center;"><%= sprint.fechafin %></td>
	<td><%= link_to '<i class="icon-edit icon-white"></i> Editar'.html_safe, edit_alumno_sprint_path(sprint), :class => 'btn btn-info' %></td>
    <td><%= link_to '<i class="icon-trash icon-white"></i> Eliminar'.html_safe, alumno_sprint_path(sprint), confirm: "¿Desea eliminar sprint: #{sprint.id}?", method: :delete, :class => 'btn btn-danger' %></td>
  </tr>
<% end %>
</tbody>
</table>

<br />

<%= link_to '<i class="icon-pencil icon-white"></i> Agregar Sprint'.html_safe, new_alumno_sprint_path, :class => 'btn btn-info' %>

